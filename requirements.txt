# Version ranges (not hard pins) so pip/uv installs prebuilt wheels across
# Python versions instead of compiling from source.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.9,<3.0
pydantic-settings>=2.5
httpx>=0.27
prometheus-client>=0.21
python-dotenv>=1.0
streamlit>=1.40,<2.0
pandas>=2.2,<3.0
plotly>=5.24

# Optional: exact token counts for the live /chat API. The code falls back to a
# heuristic if it's absent, and the dashboard doesn't need it — so it's left out
# of the default install to avoid a source build on Python versions without a
# prebuilt wheel. Install separately if you want precise token estimates:
#   pip install tiktoken
